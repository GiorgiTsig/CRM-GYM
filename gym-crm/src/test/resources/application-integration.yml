spring:
  datasource:
    url: jdbc:h2:mem:gym_crm_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: false

eureka:
  client:
    enabled: false

app:
  messaging:
    workload-queue: trainer.workload.queue.integration